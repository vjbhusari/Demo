#include<stdio.h>
#include<string.h>
int main()
{
    char name[100]="ajay bhusari";                     // space between 2 words has 1 size

    printf("length is %d ",strlen(name));              // strlen use to calculate length   
}